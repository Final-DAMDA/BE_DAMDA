package com.damda.back.repository;

import com.damda.back.domain.ReservationAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReservationAnswerRepository extends JpaRepository<ReservationAnswer,Long> {
}
